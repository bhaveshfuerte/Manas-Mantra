{
  "name": "manas-mantra-root",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run start --prefix backend\" \"npm run dev --prefix frontend -- --host\"",
    "build": "npm ci --prefix frontend && npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "render-build": "npm ci --prefix backend && npm ci --prefix frontend && npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
