{
  "name": "manas-mantra-root",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run start --prefix backend\" \"npm run dev --prefix frontend -- --host\"",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "postinstall": "npm install --prefix backend && npm install --prefix frontend",
    "render-build": "npm run postinstall && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
