{
  "name": "manas-mantra-root",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run dev --prefix frontend -- --host",
    "build": "npm run build --prefix frontend",
    "start": "node backend/server.js",
    "render-build": "npm install --prefix frontend && npm run build --prefix frontend && npm install --prefix backend"
  }
}
